/**
 * SandboxRepositoryAdapter - Port Adapter Implementation
 *
 * Generated by Port Adapter Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: gates
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * SandboxRepositoryAdapter
 *
 * DDD: Infrastructure adapter that implements SandboxRepository port.
 * Handles infrastructure concerns (HTTP, database, external APIs).
 * Encapsulates Ddb creation - api-server passes dynamoClient only.
 */

import type {
  SandboxRepository,
} from "@paritya/services/gates";
import type { AdapterDynamoDBClient } from "../_shared/dynamodb-client-types.js";
import { SandboxRepositoryDdb } from "./sandbox-repository.ddb.js";

/**
 * SandboxRepositoryAdapter
 *
 * Implements SandboxRepository port for infrastructure layer.
 */
export class SandboxRepositoryAdapter implements SandboxRepository {
  private readonly ddb: SandboxRepositoryDdb;

  constructor(
    private readonly dynamoClient: AdapterDynamoDBClient
  ) {
    this.ddb = new SandboxRepositoryDdb(this.dynamoClient);
  }

  async listSandboxPermits(input: Parameters<SandboxRepository['listSandboxPermits']>[0]): Promise<Awaited<ReturnType<SandboxRepository['listSandboxPermits']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.listSandboxPermits(input);
  }

  async getSandboxPermit(input: Parameters<SandboxRepository['getSandboxPermit']>[0]): Promise<Awaited<ReturnType<SandboxRepository['getSandboxPermit']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.getSandboxPermit(input);
  }
}
