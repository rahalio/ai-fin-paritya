/**
 * UpdateAppealPath handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: appeals
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { updateAppealPath } from "@paritya/api-server/domains/appeals/handlers";
import type { AppealsDDDDependencies } from "@paritya/api-server/domains/appeals";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@paritya/tests/shared";
import { APPEALS_USE_CASES } from "../__factories__/use-cases.js";

describe("UpdateAppealPath", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: AppealsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<AppealsDDDDependencies>(
      APPEALS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with updated entity", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const input = {} as any;
    const updatedEntity = { ...entity, ...input };
    const useCaseResult = { data: updatedEntity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.params = { appealPathId: entity.id };
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeUpdateAppealPath, "execute").mockResolvedValue(useCaseResult);

    await updateAppealPath(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeUpdateAppealPath.execute).toHaveBeenCalledWith(expect.objectContaining({ appealPathId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
