/**
 * GetAppealPath handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: appeals
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getAppealPath } from "@paritya/api-server/domains/appeals/handlers";
import type { AppealsDDDDependencies } from "@paritya/api-server/domains/appeals";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@paritya/tests/shared";
import { APPEALS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetAppealPath", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: AppealsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<AppealsDDDDependencies>(
      APPEALS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetAppealPath, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { appealPathId: entity.id };

    await getAppealPath(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetAppealPath.execute).toHaveBeenCalledWith(expect.objectContaining({ appealPathId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { appealPathId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetAppealPath, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getAppealPath(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
