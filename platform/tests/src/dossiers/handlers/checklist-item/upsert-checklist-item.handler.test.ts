/**
 * UpsertChecklistItem handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: dossiers
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { upsertChecklistItem } from "@paritya/api-server/domains/dossiers/handlers";
import type { DossiersDDDDependencies } from "@paritya/api-server/domains/dossiers";
import { createChecklistItem as createChecklistItemEntity } from "@paritya/factories/dossiers";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@paritya/tests/shared";
import { DOSSIERS_USE_CASES } from "../__factories__/use-cases.js";

describe("UpsertChecklistItem", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: DossiersDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<DossiersDDDDependencies>(
      DOSSIERS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = {} as any;
    const entity = createChecklistItemEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeUpsertChecklistItem, "execute").mockResolvedValue(useCaseResult);

    await upsertChecklistItem(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeUpsertChecklistItem.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
