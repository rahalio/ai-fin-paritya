/**
 * GetDossier handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: dossiers
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getDossier } from "@paritya/api-server/domains/dossiers/handlers";
import type { DossiersDDDDependencies } from "@paritya/api-server/domains/dossiers";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@paritya/tests/shared";
import { DOSSIERS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetDossier", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: DossiersDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<DossiersDDDDependencies>(
      DOSSIERS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetDossier, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { dossierId: entity.id };

    await getDossier(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetDossier.execute).toHaveBeenCalledWith(expect.objectContaining({ dossierId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { dossierId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetDossier, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getDossier(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
