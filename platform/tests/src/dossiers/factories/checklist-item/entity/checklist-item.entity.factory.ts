/**
 * ChecklistItem Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: dossiers
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { faker } from "@faker-js/faker";
import type { ChecklistItemEntity } from "@paritya/core/dossiers/models/index.js";
import { dossiersSchemas } from "@paritya/core/dossiers";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "dos";

export function createChecklistItem(overrides: Partial<ChecklistItemEntity> = {}) {
  const raw = {
    id: overrides.id ?? `cli_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    code: overrides.code ?? faker.lorem.word(),
    dossierId: overrides.dossierId ?? `dos_${ulid().toLowerCase()}`,
    label: overrides.label ?? faker.lorem.word(),
    status: overrides.status ?? "pending",
    ...overrides,
  };

  return dossiersSchemas.ChecklistItem.parse(raw);
}

export function checklistItemBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createChecklistItem(overrides));
}
