/**
 * GetSandboxPermit handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: gates
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getSandboxPermit } from "@paritya/api-server/domains/gates/handlers";
import type { GatesDDDDependencies } from "@paritya/api-server/domains/gates";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@paritya/tests/shared";
import { GATES_USE_CASES } from "../__factories__/use-cases.js";

describe("GetSandboxPermit", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: GatesDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<GatesDDDDependencies>(
      GATES_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetSandboxPermit, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { sandboxPermitId: entity.id };

    await getSandboxPermit(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetSandboxPermit.execute).toHaveBeenCalledWith(expect.objectContaining({ sandboxPermitId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { sandboxPermitId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetSandboxPermit, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getSandboxPermit(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
