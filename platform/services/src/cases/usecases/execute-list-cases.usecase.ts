/**
 * ExecuteListCases - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: cases
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteListCases Use Case
 *
 * DDD: Application use case for list Case.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ListCasesInput, ListCasesOutput } from "../dto/case.dto";
import type { ExecutionContextService, IdGeneratorService } from "@paritya/services/_shared/index.js";
import type { CaseRepository } from "../ports";

export class ExecuteListCases {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly caseRepo: CaseRepository
  ) {}

  async execute(input: ListCasesInput): Promise<ListCasesOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.casId();

    // All field names extracted from OpenAPI spec: none
    // List entities via repository
    const result = await this.caseRepo.listCases({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return result as ListCasesOutput;
  }
}
