/**
 * ExecuteUpdateAppealPath - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: appeals
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteUpdateAppealPath Use Case
 *
 * DDD: Application use case for update Appeal.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { UpdateAppealPathInput, UpdateAppealPathOutput } from "../dto/appeal.dto";
import type { ExecutionContextService, IdGeneratorService } from "@paritya/services/_shared/index.js";
import type { AppealRepository } from "../ports";
import { PermissionDeniedError, ValidationError } from "../errors";
import { CanUpdate } from "../policies/can-update.policy";
export class ExecuteUpdateAppealPath {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly appeal: AppealRepository
  ) {}

  async execute(input: UpdateAppealPathInput): Promise<UpdateAppealPathOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.aplId();

    // All field names extracted from OpenAPI spec
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).appealPathId) {
      throw new ValidationError("appealPathId is required");
    }
    const appealpathId = (input as any).appealPathId;
    // No get/list operation for this resource - call update directly
    // Check policy
    if (!CanUpdate.allow({} as any, "")) {
      throw new PermissionDeniedError("Operation not allowed");
    }

    // Update entity via repository (pass correlation ID for response meta)
    const updated = await this.appeal.updateAppealPath({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return updated as UpdateAppealPathOutput;
  }
}
