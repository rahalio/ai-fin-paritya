/**
 * ExecuteGetAppealPath - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: appeals
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetAppealPath Use Case
 *
 * DDD: Application use case for get Appeal.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetAppealPathInput, GetAppealPathOutput } from "../dto/appeal.dto";
import type { ExecutionContextService, IdGeneratorService } from "@paritya/services/_shared/index.js";
import type { AppealRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetAppealPath {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly appeal: AppealRepository
  ) {}

  async execute(input: GetAppealPathInput): Promise<GetAppealPathOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.aplId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).appealPathId) {
      throw new ValidationError("appealPathId is required");
    }
    const appealpathId = (input as any).appealPathId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.appeal.getAppealPath({ appealPathId: appealpathId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${appealpathId}`);
    }

    return entity as GetAppealPathOutput;
  }
}
