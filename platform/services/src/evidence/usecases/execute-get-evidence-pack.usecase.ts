/**
 * ExecuteGetEvidencePack - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: evidence
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetEvidencePack Use Case
 *
 * DDD: Application use case for get EvidencePack.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetEvidencePackInput, GetEvidencePackOutput } from "../dto/evidence-pack.dto";
import type { ExecutionContextService, IdGeneratorService } from "@paritya/services/_shared/index.js";
import type { EvidencePackRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetEvidencePack {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly evidencePack: EvidencePackRepository
  ) {}

  async execute(input: GetEvidencePackInput): Promise<GetEvidencePackOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.evdId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).evidencePackId) {
      throw new ValidationError("evidencePackId is required");
    }
    const evidencepackId = (input as any).evidencePackId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.evidencePack.getEvidencePack({ evidencePackId: evidencepackId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${evidencepackId}`);
    }

    return entity as GetEvidencePackOutput;
  }
}
