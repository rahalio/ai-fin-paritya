/**
 * ExecuteGetPlaybook - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: playbooks
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetPlaybook Use Case
 *
 * DDD: Application use case for get Playbook.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetPlaybookInput, GetPlaybookOutput } from "../dto/playbook.dto";
import type { ExecutionContextService, IdGeneratorService } from "@paritya/services/_shared/index.js";
import type { PlaybookRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetPlaybook {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly playbook: PlaybookRepository
  ) {}

  async execute(input: GetPlaybookInput): Promise<GetPlaybookOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.plbId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).playbookId) {
      throw new ValidationError("playbookId is required");
    }
    const playbookId = (input as any).playbookId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.playbook.getPlaybook({ playbookId: playbookId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${playbookId}`);
    }

    return entity as GetPlaybookOutput;
  }
}
