/**
 * ExecuteGetSandboxPermit - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: gates
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetSandboxPermit Use Case
 *
 * DDD: Application use case for get Sandbox.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetSandboxPermitInput, GetSandboxPermitOutput } from "../dto/sandbox.dto";
import type { ExecutionContextService, IdGeneratorService } from "@paritya/services/_shared/index.js";
import type { SandboxRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetSandboxPermit {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly sandbox: SandboxRepository
  ) {}

  async execute(input: GetSandboxPermitInput): Promise<GetSandboxPermitOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.gatId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).sandboxPermitId) {
      throw new ValidationError("sandboxPermitId is required");
    }
    const sandboxpermitId = (input as any).sandboxPermitId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.sandbox.getSandboxPermit({ sandboxPermitId: sandboxpermitId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${sandboxpermitId}`);
    }

    return entity as GetSandboxPermitOutput;
  }
}
