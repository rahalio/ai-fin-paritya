/**
 * ExecuteListSandboxPermits - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: gates
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteListSandboxPermits Use Case
 *
 * DDD: Application use case for list Sandbox.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ListSandboxPermitsInput, ListSandboxPermitsOutput } from "../dto/sandbox.dto";
import type { ExecutionContextService, IdGeneratorService } from "@paritya/services/_shared/index.js";
import type { SandboxRepository } from "../ports";

export class ExecuteListSandboxPermits {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly sandbox: SandboxRepository
  ) {}

  async execute(input: ListSandboxPermitsInput): Promise<ListSandboxPermitsOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.gatId();

    // All field names extracted from OpenAPI spec: none
    // List entities via repository
    const result = await this.sandbox.listSandboxPermits({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return result as ListSandboxPermitsOutput;
  }
}
