/**
 * Playbooks DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: playbooks
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Playbooks Domain Module - Composition root
 *
 * DDD: Composition root for playbooks domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  PlaybookRepositoryAdapter,
} from "@paritya/adapters/playbooks";
import { getIdGeneratorService } from "@paritya/adapters";
import type { AdapterDynamoDBClient } from "@paritya/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteCreatePlaybook,
  ExecuteGetPlaybook,
  ExecuteListPlaybooks,
} from "@paritya/services/playbooks/usecases";
// Ports (interfaces)
import type {
  PlaybookRepository,
} from "@paritya/services/playbooks/ports";

export interface PlaybooksDomainModule {
  repos: {
    playbooks: PlaybookRepository;
  };
  useCases: {
    playbooks: {
      create: ExecuteCreatePlaybook;
      get: ExecuteGetPlaybook;
      list: ExecuteListPlaybooks;
    };
  };
}

export function buildPlaybooksDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): PlaybooksDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      playbooks?: PlaybookRepository;
    } = {};

    obj.playbooks = new PlaybookRepositoryAdapter(dynamoClient);
    return {
      playbooks: obj.playbooks!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    playbooks: {
      create: new ExecuteCreatePlaybook(executionContext, idGenerator, repos.playbooks),
      get: new ExecuteGetPlaybook(executionContext, idGenerator, repos.playbooks),
      list: new ExecuteListPlaybooks(executionContext, idGenerator, repos.playbooks),
    },
  };
  return { repos, useCases: useCases as PlaybooksDomainModule["useCases"] };
}
